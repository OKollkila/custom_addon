<odoo>
    <data>
        <record id="action_medical_audits" model="ir.actions.act_window">
            <field name="name">Medical Audit</field>
            <field name="res_model">clinizone.medical_audit</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="view_medical_audit_tree"/>
        </record>

        <record id="action_create_leads_in_crm" model="ir.actions.server">
                <field name="name">Create Leads in CRM from next visit</field>
                <field name="model_id" ref="model_clinizone_medical_audit"/>
                <field name="binding_model_id" ref="model_clinizone_medical_audit"/>
                <field name="state">code</field>
                <field name="code"><![CDATA[
        records.action_create_leads_in_crm_py()
                ]]></field>
        </record>
    </data>
</odoo>