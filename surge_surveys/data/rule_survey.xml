<odoo>
    <record id="rule_survey_team" model="ir.rule">
        <field name="name">Survey Team</field>
        <field name="model_id" ref="survey.model_survey_survey"/>
        <field name="domain_force">['|', ('team_id', '=', False), ('team_id.member_ids', 'in', [user.id])]</field>
    </record>
</odoo>